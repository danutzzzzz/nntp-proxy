# nntp-proxy Docker Configuration

This folder contains the configuration file that gets copied into the
container at build time.

Modify `config.yaml` to match your NNTP server settings.

Example run:

